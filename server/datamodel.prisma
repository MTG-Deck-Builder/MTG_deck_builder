type User {
  id: ID! @id
  username: String!
  password: String!
  created_at: @created_at
}

type Decklist {
  id: ID! @id
  deck_name: String!
  user: User @relation(link: INLINE)
}

type Card {
  id: ID! @id
  card_name: String!
  image: String!
}


